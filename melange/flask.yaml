package:
  name: py3-flask
  version: latest
  epoch: 0
  description: "Python package Flask"
  target-architecture:
    - x86_64
    - aarch64

environment:
  contents:
    packages:
      - python3
      - py3-pip
      - py3-setuptools
      - py3-wheel
      - gcc
      - musl-dev

pipeline:
  - name: Build Flask wheel
    runs: |
      pip wheel Flask --wheel-dir /package
      pip install --no-index --find-links=/package Flask --target=/app

data:
  paths:
    - path: /app
      type: tree
