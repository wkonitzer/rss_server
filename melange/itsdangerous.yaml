package:
  name: py3-itsdangerous
  version: latest
  epoch: 0
  description: "Python package itsdangerous"
  target-architecture:
    - x86_64
    - aarch64
  copyright:
    - license: BSD-3-Clause

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - python3
      - py3-pip
      - py3-setuptools
      - py3-wheel
      - build-base

pipeline:
  - name: Build itsdangerous wheel
    runs: |
      pip wheel itsdangerous --wheel-dir /package
      pip install --no-index --find-links=/package itsdangerous --prefix "${{targets.destdir}}"
