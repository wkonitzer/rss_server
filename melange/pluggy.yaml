package:
  name: py3-pluggy
  version: latest
  epoch: 0
  description: "Python package pluggy"
  target-architecture:
    - x86_64
    - aarch64
  copyright:
    - license: MIT

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - python3
      - py3-pip
      - py3-setuptools
      - py3-wheel
      - build-base

pipeline:
  - name: Build pluggy wheel
    runs: |
      pip wheel pluggy --wheel-dir /package
      pip install --no-index --find-links=/package pluggy --prefix "${targets.destdir}"
