package:
  name: py3-click
  version: latest
  epoch: 0
  description: "Python package click"
  target-architecture:
    - x86_64
    - aarch64

environment:
  contents:
    packages:
      - python3
      - py3-pip
      - py3-setuptools
      - py3-wheel
      - gcc
      - musl-dev

pipeline:
  - runs: |
      pip wheel click --wheel-dir /package
      pip install --no-index --find-links=/package click --target=/app

data:
  paths:
    - path: /app
