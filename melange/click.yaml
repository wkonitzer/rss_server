package:
  name: py3-click
  version: latest
  epoch: 0
  description: "Python package click"
  target-architecture:
    - x86_64
    - aarch64
  copyright:
    - license: BSD-3-Clause

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - python3
      - py3-pip
      - py3-setuptools
      - py3-wheel
      - build-base

pipeline:
  - name: Build click wheel
    runs: |
      pip wheel click --wheel-dir /package
      pip install --no-index --find-links=/package click --prefix "${targets.destdir}"
