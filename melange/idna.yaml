package:
  name: py3-idna
  version: latest
  epoch: 0
  description: "Python package idna"
  target-architecture:
    - x86_64
    - aarch64

environment:
  contents:
    packages:
      - python3
      - py3-pip
      - py3-setuptools
      - py3-wheel
      - gcc
      - musl-dev

pipeline:
  - name: Build idna wheel
    runs: |
      pip wheel idna --wheel-dir /package
      pip install --no-index --find-links=/package idna --target=/app

data:
  paths:
    - path: /app
      type: tree
