package:
  name: py3-apscheduler
  version: latest
  epoch: 0
  description: "Python package APScheduler"
  target-architecture:
    - x86_64
    - aarch64

environment:
  contents:
    packages:
      - python3
      - py3-pip
      - py3-setuptools
      - py3-wheel
      - gcc
      - musl-dev

pipeline:
  - runs: |
      pip wheel APScheduler --wheel-dir /package
      pip install --no-index --find-links=/package APScheduler --target=/app

data:
  paths:
    - path: /app
