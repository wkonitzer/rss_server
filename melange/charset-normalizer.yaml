package:
  name: py3-charset-normalizer
  version: latest
  epoch: 0
  description: "Python package charset-normalizer"
  target-architecture:
    - x86_64
    - aarch64
  copyright:
    - license: MIT

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/bootstrap/stage3
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/bootstrap/stage3/wolfi-signing.rsa.pub
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - python3
      - py3-pip
      - py3-setuptools
      - py3-wheel
      - build-base

pipeline:
  - name: Build charset-normalizer wheel
    runs: |
      pip wheel charset-normalizer --wheel-dir /package
      pip install --no-index --find-links=/package charset-normalizer --prefix "${{targets.destdir}}"
